$IF QB64CC_SYSTEM_COLOR_GRAY=UNDEFINED THEN
$LET QB64CC_SYSTEM_COLOR_GRAY=DEFINED

$IF QB64CC_SYSTEM_COLOR=UNDEFINED THEN
$ERROR expected: 'Color.bi'
$ENDIF

function Color.GRAY&()
	Color.GRAY = Color.of(127, 127, 127)
end function

$ENDIF

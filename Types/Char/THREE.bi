$IF QBCC_TYPES_CHAR_THREE=UNDEFINED THEN
$LET QBCC_TYPES_CHAR_THREE=DEFINED

$IF QBCC_TYPES_CHAR=UNDEFINED THEN
$ERROR expected: 'Char.bi'
$ENDIF

function Char.THREE$()
	Char.THREE = Char.from(51)
end function

$ENDIF

$IF QB64CC_TYPES_CHAR_AT=UNDEFINED THEN
$LET QB64CC_TYPES_CHAR_AT=DEFINED

$IF QB64CC_TYPES_CHAR=UNDEFINED THEN
$ERROR expected: 'Char.bi'
$ENDIF

function Char.AT$()
	Char.AT = Char.from(64)
end function

$ENDIF

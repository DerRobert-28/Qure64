$IF QBCC_TYPES_CHAR_SPACE=UNDEFINED THEN
$LET QBCC_TYPES_CHAR_SPACE=DEFINED

$IF QBCC_TYPES_CHAR=UNDEFINED THEN
$ERROR expected: 'Char.bi'
$ENDIF

function Char.SPACE$()
	Char.SPACE = Char.from(32)
end function

$ENDIF
